# 03 — pet-core：纯逻辑 TS 移植（含 (agent, session) 复合键）

**What to build:** 把宠物包解析、状态机、多会话、动画、图集尺寸等纯逻辑以 TypeScript 移植到共享核心包，并把会话身份从"会话 id"升级为"(来源工具, 会话 id)"复合身份，让多个来源工具接入互不冲突；现有测试移植并跑通。

**Blocked by:** 01 — 单仓库脚手架与分支

**Status:** ready-for-agent

- [ ] 各纯逻辑模块以 TS 实现，可从共享核心包导入。
- [ ] 状态机/多会话按 (agent, session) 复合键工作：不同来源工具的相同会话 id 互不干扰。
- [ ] 现有测试套件（宠物包格式、状态机、多会话、动画、图集尺寸）移植后全部通过。
- [ ] 多会话优先级与"受阻已确认"等既有语义在复合键下保持正确。
