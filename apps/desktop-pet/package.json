{
  "name": "@yshark/desktop-pet",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri 2 + Vite + native TypeScript desktop pet shell.",
  "type": "module",
  "scripts": {
    "predev": "npm run build --workspace @yshark/pet-core && npm run build --workspace @yshark/pet-protocol",
    "dev": "vite",
    "prebuild": "npm run build --workspace @yshark/pet-core && npm run build --workspace @yshark/pet-protocol",
    "build": "tsc --noEmit && vite build",
    "build:widget": "vite build --config vite.widget.config.ts",
    "build:all": "npm run build && npm run build:widget",
    "preview": "vite preview",
    "tauri": "tauri",
    "pretest": "npm run build --workspace @yshark/pet-core && npm run build --workspace @yshark/pet-protocol",
    "test": "tsx --test test/*.test.ts",
    "pretypecheck": "npm run build --workspace @yshark/pet-core && npm run build --workspace @yshark/pet-protocol",
    "typecheck": "tsc --noEmit",
    "preserver": "npm run build --workspace @yshark/pet-core && npm run build --workspace @yshark/pet-protocol",
    "server": "tsx src/server-entry.ts",
    "clean": "rm -rf dist dist-ts"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@yshark/pet-core": "0.1.0",
    "@yshark/pet-protocol": "0.1.0",
    "adm-zip": "^0.6.0",
    "sharp": "^0.35.4",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
