# 桌宠托盘 手动测试用例

> 适用版本：DSH 0.1.2-alpha.3 + 本仓库当前代码（已合入 DSH 适配修复 + 托盘交互改版）。
> 测试面：**Windows 原生桌宠**（desktop-pet.exe，透明窗口）为主；也可用浏览器预览
> `http://127.0.0.1:1420/`（宠物窗）。
>
> 勾选列：`[ ]` 未测 / `[x]` 通过 / `[x]✗` 不通过（附现象）。
>
> **当前交互设计（无气泡）**：气泡功能已移除。有活动会话时托盘**自动展开一次**；
> 可手动关闭（点 ▲ 角标），关闭后显示 **▼ 小三角角标（含活动数量）**；无活动时托盘与角标全部消失。

## 0. 前置确认（每次开测前）

- [ ] `dsh web` 在跑（3080），`http://127.0.0.1:3080/pet/bridge/status` 返回 `"connected":true`
- [ ] pet server 在跑（3720），Vite 在跑（1420）
- [ ] Windows 桌宠进程在跑（任务管理器可见 desktop-pet.exe）
- [ ] 桌宠窗口可见；右键宠物 → 菜单出现（设置/隐藏）

---

## 1. 活动托盘（宠物窗顶部·宠物头上方）

> 托盘 = 宠物窗**顶部居中（宠物头上方）**的列表浮层，显示所有已激活会话：标题 + `来源 · 状态标签`。
> 锚定在顶部的**胶囊角标 = 活动数量 + 纯 CSS 三角**：收起时 ▼、展开时 ▲，点击开/关。
> 状态标签：`需要输入`(waiting) / `受阻`(blocked,failed) / `就绪`(ready) / `运行中`(running,working) / `空闲`。

### TC-T1 有活动时托盘自动展开
- 步骤：确保 ≥1 个 DSH 会话有活动（本会话工具调用即算）；打开/刷新宠物窗。
- 期望：托盘列表**自动展开**（无需点击），顶部角标显示 **数量 + ▲**（如 `2 ▲`）。
- 结果：[ ]

### TC-T2 无活动时托盘与角标消失
- 步骤：等所有活动结束/空闲，或 §4 注入脚本结束后观察。
- 期望：托盘列表与 ▲/▼ 角标全部消失。
- 结果：[ ]

### TC-T3 手动关闭 → ▼ 角标 → 重新打开
- 步骤：① 托盘展开时点击 **▲**；② 再点击 **▼**。
- 期望：① 托盘收起，角标变为 **数量 + ▼**（如 `2 ▼`，提示可打开）；② 托盘重新展开，角标变回 `2 ▲`。
- 注意：角标与列表在**真实鼠标点击**下都能开/关（已修复 stage setPointerCapture 重定向点击的 bug）。
- 结果：[ ]

### TC-T4 手动关闭后不被新活动自动弹开
- 步骤：① 手动关闭托盘（▼）；② 触发一次新的工具调用/活动推送。
- 期望：托盘**保持关闭**（只显示 ▼ 角标），不会自动弹开。
- 结果：[ ]

### TC-T5 列表项状态标签正确
- 步骤：用 §4 注入脚本同时注入 running / question / approval / error 四类。
- 期望：对应标签为 `运行中` / `需要输入` / `需要输入` / `受阻`，来源显示 `fake`；未读项有蓝点。
- 结果：[ ]

### TC-T6 点击项标记已读 + 打开 DSH 会话
- 步骤：① 展开托盘，点击某个 `运行中` 的 dsh 会话项；② 切到 DSH GUI 观察。
- 期望：① 该项蓝点消失（已读），托盘自动收起；② DSH GUI 切换到该会话页面。
- 结果：[ ]

### TC-T7 已读不再提醒
- 步骤：TC-T6 点击后，保持该会话无新活动，再看托盘。
- 期望：该会话不再出现在托盘列表（已读的 blocked/ready 也 `reminder=false`）。
- 结果：[ ]

### TC-T8 多会话聚合
- 步骤：让 2 个以上会话有活动（真实 + §4 注入）。
- 期望：托盘列出全部；列表按优先级排序（需要输入/受阻 在前，运行中在后）。
- 结果：[ ]

### TC-T9 断连清理
- 步骤：停掉 pet server 再重启（或 kill 注入脚本）。
- 期望：对应来源的活动从托盘消失（断连会 sync 清空该 agent 缓存）。
- 结果：[ ]

---

## 2. 系统托盘（Windows 任务栏，右键）

> 注意：Win11 新应用图标默认进"隐藏图标"溢出区——先点任务栏 `^` 展开，右键"桌宠"→"固定到任务栏"或直接拖出，之后才能看到。

### TC-ST1 图标与 tooltip
- 步骤：展开隐藏图标区，悬停桌宠图标。
- 期望：显示 tooltip `桌宠`。
- 结果：[ ]

### TC-ST2 右键菜单三项
- 步骤：右键桌宠图标。
- 期望：菜单含 `唤醒/隐藏`、`设置`、`退出`。
- 结果：[ ]

### TC-ST3 唤醒/隐藏
- 步骤：① 宠物窗可见 → 点 `隐藏`；② 点 `唤醒/隐藏`。
- 期望：① 宠物窗消失；② 宠物窗重新出现。
- 结果：[ ]

### TC-ST4 设置
- 步骤：点 `设置`；然后关闭设置窗，再点一次 `设置`。
- 期望：设置窗每次都能重新打开（关闭=隐藏，非销毁，回归点）。
- 结果：[ ]

### TC-ST5 左键不弹菜单
- 步骤：左键单击桌宠图标。
- 期望：不弹菜单（`show_menu_on_left_click(false)`）。
- 结果：[ ]

### TC-ST6 退出
- 步骤：点 `退出`。
- 期望：桌宠应用整体退出（进程结束）。测完重新启动即可。
- 结果：[ ]

---

## 3. 状态对照速查表

| DSH 状态 | 托盘状态标签 |
|---|---|
| 思考中 | 运行中 |
| 使用工具 | 运行中 |
| 索要权限 | 需要输入 |
| 提问 | 需要输入 |
| 错误 | 受阻 |
| 空闲 | （不显示） |

---

## 4. 辅助：注入脚本（模拟审批/提问/错误状态）

真实触发审批/提问不方便时，用这个脚本向 pet server 注入合成活动（桥接协议），
桌宠会像收到真实 DSH 事件一样显示托盘。用法：

```bash
node /home/weikang/dsh-pet-plugin/scripts/simulate-pet-states.mjs
# 脚本会：连上 /v1，发 hello + running/question/approval/error 四类活动，保持 60 秒后退出
# 保持期间去桌宠/浏览器里观察托盘；断开后 fake 来源的活动自动清理
```

---

## 5. 发现问题时记录模板

```
- 用例编号：
- 实际现象（截图更好，存 shot-*.png）：
- 期望 vs 实际差异：
- 相关日志（pet server 输出 / DSH GUI 控制台）：
```

> 注意：Windows 桌宠是透明窗口，PrintWindow 截图黑底是假象，不代表真实渲染。
> 视觉复核可用：`opencode run -m opencode-go/mimo-v2.5 "描述…" -f <截图>`。
