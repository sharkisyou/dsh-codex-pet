{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the desktop pet windows",
  "windows": ["pet", "settings"],
  "permissions": [
    "core:default"
  ]
}
