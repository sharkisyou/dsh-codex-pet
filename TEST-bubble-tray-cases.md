# 桌宠气泡 + 托盘 手动测试用例

> 适用版本：DSH 0.1.2-alpha.3 + 本仓库当前代码（已合入 DSH 适配修复）。
> 测试面：**Windows 原生桌宠**（desktop-pet.exe，透明窗口）为主；也可用浏览器预览
> `http://127.0.0.1:1420/`（宠物窗）、`http://127.0.0.1:1420/?window=settings`（设置窗）。
>
> 勾选列：`[ ]` 未测 / `[x]` 通过 / `[x]✗` 不通过（附现象）。

## 0. 前置确认（每次开测前）

- [ ] `dsh web` 在跑（3080），`http://127.0.0.1:3080/pet/bridge/status` 返回 `"connected":true`
- [ ] pet server 在跑（3720），Vite 在跑（1420）
- [ ] Windows 桌宠进程在跑（任务管理器可见 desktop-pet.exe）
- [ ] 桌宠窗口可见；右键宠物 → 菜单出现（设置/隐藏）

---

## 1. 气泡（Speech Bubble）

> 气泡 = 宠物上方的小浮层，显示 `dsh` + 状态文案。
> **当前行为：气泡随活动状态常驻**（有活动一直显示、状态变更即时更新，活动回到空闲时才消失；不再 8.5 秒自动消失）。
> 气泡文案与状态对照（`BUBBLE_TEXT`）：

| bubbleKey | 气泡文案 |
|---|---|
| `thinking` | 思考中 |
| `executingTool` | 执行工具: `<工具名>`（如 执行工具: bash） |
| `waitingApproval` | 等待批准 |
| `waitingAnswer` | 等待回答 |
| `subagentWorking` | 子代理工作中 |
| `failed` | 失败 |
| `awaitingReply` | 等待回复 |

### TC-B1 工具执行时显示气泡
- 步骤：在 DSH GUI 发起一个会跑工具的会话（例如"用 bash 打印当前目录文件列表"）。
- 期望：桌宠窗口顶部出现气泡，标题 `dsh`，文案 `执行工具: bash`；气泡保持显示（不自动消失）。
- 结果：[ ]

### TC-B2 思考中显示气泡
- 步骤：发起一个模型思考较长的任务（不给明确工具步骤）。
- 期望：气泡文案 `思考中`（若太快可多次观察）。
- 结果：[ ]

### TC-B3 等待回答/等待批准时气泡
- 步骤：触发一次 ask_user_question 或审批（见 §5 注入脚本，或真实触发）。
- 期望：气泡文案分别为 `等待回答` / `等待批准`。
- 结果：[ ]

### TC-B4 气泡不自动隐藏、空闲时消失
- 步骤：TC-B1 触发后不操作，观察。
- 期望：气泡保持显示超过 8.5 秒；活动回到空闲后气泡消失，宠物回到空闲/待命状态。
- 结果：[ ]

### TC-B5 会话空闲时无气泡
- 步骤：所有 DSH 会话无活动时观察。
- 期望：无气泡；`#pet-status`（状态栏）显示 `来源：dsh` 或 `等待桥接连接`（断连时）。
- 结果：[ ]

### TC-B6 多个来源并存时气泡取当前活动
- 步骤：让 DSH 会话工作（执行工具）+ 用 §5 注入脚本注入一个 `question/start`。
- 期望：气泡显示当前优先级最高活动对应文案（优先级：等待 > 运行）。
- 结果：[ ]

---

## 2. 活动托盘（宠物窗内 "活动 (N)"）

> 数据来自 pet server `trayActivities()`（`reminder=true` 的活动）。状态标签：
> `需要输入`(waiting) / `受阻`(blocked,failed) / `就绪`(ready) / `运行中`(running,working) / `空闲`。

### TC-T1 有活动时按钮出现
- 步骤：确保 ≥1 个 DSH 会话有活动（本会话工具调用即算）。
- 期望：宠物窗右下角出现圆角按钮 `活动 (1)`（或更大数字），半透明深色底、白字。
- 结果：[ ]

### TC-T2 无活动时按钮隐藏
- 步骤：等所有活动结束/空闲，或 §5 注入脚本结束后观察。
- 期望：`活动` 按钮消失（`hidden`）。
- 结果：[ ]

### TC-T3 点击按钮展开列表
- 步骤：点击 `活动 (N)`。
- 期望：按钮上方弹出下拉列表；每项 = 标题（会话标题或 id）+ `来源 · 状态标签`；未读项右侧有蓝点。
- 结果：[ ]

### TC-T4 列表项状态标签正确
- 步骤：用 §5 注入脚本同时注入 running / question / approval / error 四类。
- 期望：对应标签为 `运行中` / `需要输入` / `需要输入` / `受阻`，且来源显示 `fake`。
- 结果：[ ]

### TC-T5 点击项标记已读 + 打开 DSH 会话
- 步骤：① 展开托盘，点击某个 `运行中` 的 dsh 会话项；② 切到 DSH GUI 观察。
- 期望：① 该项蓝点消失（已读），托盘自动收起；② DSH GUI 切换到该会话页面。
- 结果：[ ]

### TC-T6 已读不再提醒
- 步骤：TC-T5 点击后，保持该会话无新活动，再看托盘。
- 期望：该会话不再出现在托盘列表（已读的 blocked/ready 也 `reminder=false`）。
- 结果：[ ]

### TC-T7 多会话聚合
- 步骤：让 2 个以上会话有活动（真实 + §5 注入）。
- 期望：按钮数字 = 活动总数；列表按优先级排序（需要输入/受阻 在前，运行中在后）。
- 结果：[ ]

### TC-T8 断连清理
- 步骤：停掉 pet server 再重启（或 kill 注入脚本）。
- 期望：对应来源的活动从托盘消失（断连会 sync 清空该 agent 缓存）。
- 结果：[ ]

---

## 3. 系统托盘（Windows 任务栏，右键）

> 注意：Win11 新应用图标默认进"隐藏图标"溢出区——先点任务栏 `^` 展开，右键"桌宠"→"固定到任务栏"或直接拖出，之后才能看到。

### TC-ST1 图标与 tooltip
- 步骤：展开隐藏图标区，悬停桌宠图标。
- 期望：显示 tooltip `桌宠`。
- 结果：[ ]

### TC-ST2 右键菜单三项
- 步骤：右键桌宠图标。
- 期望：菜单含 `唤醒/隐藏`、`设置`、`退出`。
- 结果：[ ]

### TC-ST3 唤醒/隐藏
- 步骤：① 宠物窗可见 → 点 `隐藏`；② 点 `唤醒/隐藏`。
- 期望：① 宠物窗消失；② 宠物窗重新出现。
- 结果：[ ]

### TC-ST4 设置
- 步骤：点 `设置`；然后关闭设置窗，再点一次 `设置`。
- 期望：设置窗每次都能重新打开（关闭=隐藏，非销毁，回归点）。
- 结果：[ ]

### TC-ST5 左键不弹菜单
- 步骤：左键单击桌宠图标。
- 期望：不弹菜单（`show_menu_on_left_click(false)`）；有活动时宠物窗保持现状。
- 结果：[ ]

### TC-ST6 退出
- 步骤：点 `退出`。
- 期望：桌宠应用整体退出（进程结束）。测完重新启动即可。
- 结果：[ ]

---

## 4. 状态对照速查表（气泡 vs 托盘）

| DSH 状态 | 气泡 | 托盘标签 |
|---|---|---|
| 思考中 | 思考中 | 运行中 |
| 使用工具 | 执行工具: bash | 运行中 |
| 索要权限 | 等待批准 | 需要输入 |
| 提问 | 等待回答 | 需要输入 |
| 错误 | 失败 | 受阻 |
| 空闲 | （无） | （不显示） |

---

## 5. 辅助：注入脚本（模拟审批/提问/错误状态）

真实触发审批/提问不方便时，用这个脚本向 pet server 注入合成活动（桥接协议），
桌宠会像收到真实 DSH 事件一样显示。用法：

```bash
node /home/weikang/dsh-pet-plugin/scripts/simulate-pet-states.mjs
# 脚本会：连上 /v1，发 hello + running/question/approval/error 四类活动，保持 60 秒后退出
# 保持期间去桌宠/浏览器里观察气泡与托盘
```

脚本源码见仓库 `scripts/simulate-pet-states.mjs`。结束（断开）后 `fake` 来源的活动会自动清理。

---

## 6. 发现问题时记录模板

```
- 用例编号：
- 实际现象（截图更好，存 shot-*.png）：
- 期望 vs 实际差异：
- 相关日志（pet server 输出 / DSH GUI 控制台）：
```

> 注意：Windows 桌宠是透明窗口，PrintWindow 截图黑底是假象，不代表真实渲染。
> 视觉复核可用：`opencode run -m opencode-go/mimo-v2.5 "描述…" -f <截图>`。
