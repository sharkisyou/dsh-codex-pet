{
  "name": "@yshark/desktop-pet",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri 2 + Vite + native TypeScript desktop pet shell.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist dist-ts"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
